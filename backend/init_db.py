# ============================================
# ZeroCode — init_db.py
# One-off script that creates all database
# tables if they do not already exist.
# ============================================

from database import init_db

init_db()
print("Database initialized successfully!")
